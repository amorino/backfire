@import '~styles/utils/mixins'

@lost gutter 30px
@lost flexbox no-flex
@lost cycle auto

// Fonts
$font = 'BrandonText', 'Helvetica', sans-serif

// Colors
$gray =  #e3e0e7
$white = #fff
$black =  #232323

$color = $black
$background = $white

// Breakpoints
$center = 1024px
$mobile = 560px
$tablet = 768px
